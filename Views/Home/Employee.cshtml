@model CodingTest.Models.Employee

@{
	ViewData["Title"] = "Employee";
}


<h1>Employee</h1>

<div>

	@{
		if (Context.Request.Query["employeeid"].Count > 0)
		{
			Employee? _employee = CodingTest.Controllers.HomeController.GetEmployee(int.Parse(Context.Request.Query["employeeid"][0]));
			if (_employee == null)
			{
				<h4>Employee ID Not Found</h4>
				<a asp-action="Index">Back to List</a>
			}
			else
			{
				<h4>Employee ID @_employee.EmployeeId</h4>
				<hr />
				<dl class="row">
					<dt class="col-sm-2">
						@Html.DisplayNameFor(model => model.Name)
					</dt>
					<dd class="col-sm-10">
						@_employee.Name
					</dd>
					<dt class="col-sm-2">
						@Html.DisplayNameFor(model => model.Address)
					</dt>
					<dd class="col-sm-10">
						@_employee.Address
					</dd>
				</dl>
				<div>
					<a asp-action="Edit" asp-route-employeeid="@_employee.EmployeeId">Edit</a> |
					<a asp-action="Delete" asp-route-employeeid="@_employee.EmployeeId">Delete</a> |
					<a asp-action="Index">Back to List</a>
				</div>
			}
		}
		else
		{
			<h4>Please provide a query</h4>
			<div>
				<a asp-action="Index">Back to List</a>
			</div>
		}

	}

</div>

